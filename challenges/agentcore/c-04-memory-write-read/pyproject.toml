[project]
name = "c-04-memory-write-read"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "langgraph>=0.4.0",
  "langchain-core>=0.3.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.uv]
dev-dependencies = []
