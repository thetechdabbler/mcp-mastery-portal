# lab-01-supervisor-graph

Build a minimal LangGraph supervisor with mocked workers (local only).

## Run

```bash
uv run python src/agent.py
```

## Test

```bash
uv run pytest tests/
```

## AWS

Local only — no AWS account.
